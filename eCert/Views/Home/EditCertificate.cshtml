@model eCert.Models.ViewModel.CertificateViewModel
@using eCert.Models.ViewModel
@using eCert.Utilities
@{
    ViewBag.Title = "EditCertificate";
}
@{
    //Get certificate links
    List<CertificateContentsViewModel> links = Model.CertificateContents.Where(cert => cert.Format == Constants.CertificateFormat.LINK).ToList();
    //Get certificate files
    List<CertificateContentsViewModel> files = Model.CertificateContents
        .Where(cert => cert.Format == Constants.CertificateFormat.JPEG
            || cert.Format == Constants.CertificateFormat.JPG
            || cert.Format == Constants.CertificateFormat.PNG
            || cert.Format == Constants.CertificateFormat.PDF)
        .ToList();
}

<script src="/assets/js/vendor/jquery-3.5.1.min.js"></script>
<!-- custom js -->
<script src="~/assets/js/custom/home.js"></script>
<!-- custom js end -->
<!-- third party css -->
<link href="/assets/css/vendor/dataTables.bootstrap4.css"
      rel="stylesheet"
      type="text/css" />
<link href="/assets/css/vendor/responsive.bootstrap4.css"
      rel="stylesheet"
      type="text/css" />
<!-- third party css end -->
<!-- App css -->
<link href="/assets/css/icons.min.css" rel="stylesheet" type="text/css" />
<link href="/assets/css/app.min.css"
      rel="stylesheet"
      type="text/css"
      id="light-style" />
<link href="/assets/css/app-dark.min.css"
      rel="stylesheet"
      type="text/css"
      id="dark-style" />
<!--custom css-->
<link rel="stylesheet"
      type="text/css"
      href="/assets/css/custom/detailCertificate.css" />
<link rel="stylesheet"
      type="text/css"
      href="~/assets/css/custom/editCertificate.css" />


<div class="general">
    <!--Title-->
    <h3 class="title-cert">Edit Certificate</h3>

    <div class="card">
        <div class="card-body">
            @using (Html.BeginForm("EditCertificate", "home", FormMethod.Post, new { enctype = "multipart/form-data", @class = "" }))
            {
                <div class="row">
                    <!--First column-->
                    <div class="col-6">
                        <!--Cert name-->
                        <div class="form-group">
                            <label for="simpleinput">Certificate Name</label>
                            @Html.EditorFor(model => model.CertificateName, new { htmlAttributes = new { @class = "form-control", placeholder = "Your certificate name", onkeyup = "" } })
                            @Html.ValidationMessageFor(model => model.CertificateName, "", new { @class = "text-danger" })
                            <p class="text-danger mt-1 cert_name">Certificate name is required</p>
                        </div>
                        <!--Issue Date, Expiry Date-->
                        <div class="row px-0 mt-4">
                            <div class="form-group col-6">
                                <label for="example-date">Issue Date</label>
                                @Html.TextBoxFor(model => model.DateOfIssue, new { @type = "datetime", @class = "form-control issue-date", onchange = "hideElementByClass('.cert_date');" })
                                @*<input class="form-control" id="example-date" type="date" name="date">*@
                            </div>
                            <div class="form-group col-6">
                                <label for="example-date"> Expiry Date</label>
                                @Html.TextBoxFor(model => model.DateOfExpiry, new { @type = "datetime", @class = "form-control expiry-date", onchange = "hideElementByClass('.cert_date');" })
                                @*<input class="form-control" id="example-date" type="date" name="date">*@
                            </div>
                        </div>
                        <p class="text-danger cert_date">Issue Data have to ealier than Expiry Date</p>
                        <!--Certificate link-->
                        <div class="mt-4">
                            <label for="cert-link">Certificate link</label>
                            @if (links != null && links.Count > 0)
                            {
                            <table class="table mb-0">
                                <thead style="background-color: #D5EAF2">
                                    <tr>
                                        <th scope="col">No</th>
                                        <th scope="col">Link</th>
                                        <th scope="col"></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @for (int i = 0; i < Model.CertificateContents.Count; i++)
                                    {
                                        if (Model.CertificateContents[i].Format == Constants.CertificateFormat.LINK)
                                        {
                                            @Html.EditorFor(Model => Model.CertificateContents[i].Content);
                                            @Html.HiddenFor(Model => Model.CertificateContents[i].Format);

                                        }
                                    }
                                </tbody>
                            </table>
                            }
                        </div>

                    </div>

                    <!--Second column-->
                    <div class="col-6">
                        <!--Note-->
                        <div class="form-group mb-3">
                            <label for="simpleinput">Note</label>
                            @Html.TextAreaFor(model => model.Description, new { @class = "form-control", placeholder = "Give some description about your certificate", rows = "8", onkeyup = "" })
                            <p class="text-danger mt-1 cert_des">Description is required</p>
                        </div>
                        <!--Certificate file-->
                        <div class="">
                            <label for="cert-file">Certificate file</label>
                            <div class="mb-3 row ml-2">
                                <div class="d-flex flex-wrap">
                                    @for (int i = 0; i < Model.CertificateContents.Count; i++)
                                    {
                                        if (Model.CertificateContents[i].Format == Constants.CertificateFormat.PDF)
                                        {
                                            //Get file name
                                            string[] strArr = Model.CertificateContents[i].Content.Split('\\');
                                            string fileName = strArr[strArr.Length - 1];

                                            <div class="p-2">
                                                <!--First items-->
                                                <div class="row align-items-center">
                                                    <div class="col-auto">
                                                        <div class="avatar-sm">
                                                            <span class="avatar-title bg-light text-secondary rounded position-relative">
                                                                <i class="mdi mdi-folder-zip font-16"></i>
                                                                <button class="btn px-0 py-0 position-absolute btn-x"><i class="dripicons-cross btn-delete"></i></button>
                                                            </span>

                                                        </div>
                                                    </div>
                                                    <div class="col pl-0">
                                                        <a href="javascript:void(0);" class="text-muted font-weight-bold">@fileName</a>
                                                        @Html.EditorFor(Model => Model.CertificateContents[i].Content);
                                                        @Html.HiddenFor(Model => Model.CertificateContents[i].Format);
                                                        <p class="mb-0 font-13">MB</p>
                                                    </div>
                                                </div> <!-- end row -->
                                            </div> <!--First items-->
                                        }


                                    }

                                </div>
                            </div>
                            @*<div class="">
                                <button class="d-flex align-content-between btn" style="color: #2286FF">
                                    <i class="dripicons-paperclip"></i>
                                    <div style="margin-left:5px">Upload your attachments</div>
                                </button>
                            </div>*@
                        </div>
                        <p class="text-danger mt-1 cert_file">Certificate link or certificate file is required</p>
                        <button class="btn float-right btn-save">Save</button>
                    </div>

                </div>
            }
        </div>
    </div>
</div>



